const id=259
let name="Swasti"
var branch="cse"
city="ghaziabad"
let college
// if we use const keyword, then the value of that variable cannot be changed.
/*
prefer to use const and let instead of var 
because of issue in block scope and functional scope.
*/
console.table([id, name, branch, city, college])
